// Use the requested loading1.json for loading animation across the app
import loading from '../loading1.json';
import error from './error.json';
// Re-export known-good success animation from its current location to avoid duplication
import success from '../success (1).json';

export { loading, error, success };
